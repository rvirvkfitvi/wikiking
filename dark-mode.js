const toggleTheme = () => {
    const body = document.body;
    body.classList.toggle('dark');
};

// Add a button to the HTML to activate it
// <button onclick="toggleTheme()">Toggle Dark Mode</button>